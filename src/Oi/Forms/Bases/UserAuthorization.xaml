<Window
    x:Class="Oi.Forms.UserAuthorization"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    ResizeMode="NoResize"
    Title="OI: ENTER BYPASS CODE"
    SizeToContent="Height"
    Width="400"
    Style="{DynamicResource Style_Window}"
    WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Oi;component/Resources/SharedStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Message -->
        <TextBlock x:Name="MessageTextBox"
                 Grid.Row="0"
                 Text="{Binding Message, Mode=OneWay}"
                 TextWrapping="Wrap"
                 Margin="0,0,0,12"/>

        <!-- Call / Response, buttons on the right sized to fit the largest -->
        <Grid Grid.Row="1" Margin="0,0,0,12" Grid.IsSharedSizeScope="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="80"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Query code -->
            <TextBox x:Name="ChallengeTextBlock"
               Grid.Row="0" Grid.Column="0"
               Text="{Binding Challenge, Mode=OneWay}"
               VerticalAlignment="Center"
               Margin="0,4,8,4"/>

            <Button Grid.Row="0" Grid.Column="1"
                    Content="COPY"
                    Click="CopyButton_Click"/>

            <!-- Response entry -->
            <TextBox x:Name="ResponseTextBox"
             Grid.Row="1" Grid.Column="0"
             Margin="0,4,8,4"
             VerticalContentAlignment="Center"/>

            <Button Grid.Row="1" Grid.Column="1"
            Content="AUTHORIZE"
            Click="AuthorizeButton_Click"/>

        </Grid>

        <!-- Status text -->
        <TextBlock x:Name="StatusTextBlock"
                   Grid.Row="3"
                   Margin="0,0,0,12"
                   FontStyle="Italic"
                   Text="Status of code entry"
                   Foreground="WHITE"/>

        <!-- OK button -->
        <Button x:Name="OkButton"
                Grid.Row="4"
                Content="OK"
                Height="30"
                IsEnabled="False"
                Click="OkButton_Click"/>
    </Grid>
</Window>