<Window
    x:Class="Oi.Forms.ProtectionReview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Width="900"
    Height="450"
    ResizeMode="CanResizeWithGrip"
    Style="{DynamicResource Style_Window}"
    WindowStartupLocation="CenterScreen"
    Title="OI: PROTECTION REVIEW">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Oi;component/Resources/SharedStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <!-- Message -->
        <TextBlock
            Grid.Row="0"
            Text="{Binding Message, Mode=OneWay}"
            TextWrapping="Wrap"
            Margin="0,0,0,15"/>


        <!-- Protection rows -->
        <DataGrid
            Grid.Row="1"
            ItemsSource="{Binding Items}"
            IsReadOnly="True"
            Background="{StaticResource BackgroundBrush}"
            AutoGenerateColumns="False"
            HeadersVisibility="Column">

            <DataGrid.Columns>

                <DataGridTextColumn
                    Header="Element"
                    Binding="{Binding ElementName}"
                    Width="*"/>

                <DataGridTextColumn
                    Header="Category"
                    Binding="{Binding Category}"
                    Width="*"/>

                <DataGridTextColumn
                    Header="Protected By"
                    Binding="{Binding Protection.ProtectedBy}"
                    Width="*"/>

                <DataGridTextColumn
                    Header="Protected On"
                    Binding="{Binding Protection.ProtectedOn}"
                    Width="*"/>

                <DataGridTextColumn
                    Header="Reason"
                    Binding="{Binding Protection.Reason}"
                    Width="*"/>

            </DataGrid.Columns>

        </DataGrid>


        <!-- Buttons -->
        <StackPanel
            Grid.Row="2"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Margin="0,15,0,0">

            <Button
                Width="100"
                Margin="0,0,10,0"
                Content="Cancel"
                Click="Cancel_Click"/>

            <Button x:Name="BypassButton"
                Width="140"
                Content="Request Bypass"
                Click="Bypass_Click"/>

        </StackPanel>

    </Grid>

</Window>